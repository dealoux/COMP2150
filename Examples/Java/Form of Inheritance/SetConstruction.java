public class SetConstruction extends ListConstruction{

    //add method has a different behaviour compared to the add method in the List (parent) class
    public void add(int x)
    {
        //check if x is unique in the list or not
        //if it's unique then add it to the set otherwise do not add it
    }
}
